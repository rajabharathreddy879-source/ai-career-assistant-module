You are an expert Full-Stack Lead Engineer and System Architect. Your task is to construct a complete, production-ready, full-stack application end-to-end in this repository for an AI Career Chatbot Module powered by Google Gemini. Deploy a high-efficiency Node.js + Express backend and Vite + React frontend in Replit with zero placeholder files, stubbed methods, or incomplete implementations. Build a secure, AI-powered Web SaaS platform that acts as an intelligent AI Career Assistant—helping users analyze and improve resumes, optimize for ATS, prepare for technical/HR interviews, bridge skill gaps, generate project/learning roadmaps, and receive career guidance. Enable Supabase Auth (Email/OAuth), an interactive analytics dashboard, real-time streaming AI chat, resume-aware and job description-aware conversations, conversation history persistence, and report/roadmap exports. Use React.js (Vite), Tailwind CSS, Lucide Icons, Node.js + Express.js, Supabase (PostgreSQL & Auth), `@google/genai` SDK, and Zod. Implement `/` (Landing), `/login`, `/signup`, `/dashboard` (Protected), `/chat` (AI Career Assistant with History Sidebar), `/resumes` (Upload/Context Management), `/reports` (Roadmaps & Saved Advice), and `/settings` with dark/light mode support. User authenticates -> accesses `/dashboard` -> opens `/chat` -> posts user message alongside optional resume text, job description context, and previous chat history -> backend validates via Zod & invokes Gemini `gemini-2.5-flash` with strict system instructions -> formatted response streams to UI -> session and message records persist in Supabase under `user_id`. Support core career domains including Resume Analysis, ATS Optimization, Technical Interviewing (Node.js, React, System Design), HR Interviewing, Skill Gap Roadmapping, Project Recommendations, and Certification Guidance with priority tags (`Low`, `Medium`, `High`, `Critical`). Chat interface features suggested prompt chips (e.g., "Why is my ATS score low?", "Suggest backend projects", "Explain Node.js interview questions", "Rewrite my project description"), chat history sidebar, markdown rendering, code block syntax highlighting, copy buttons, and typing indicators validated live with Zod. Run SQL: Create `profiles` (id UUID PRIMARY KEY, email TEXT, full_name TEXT, created_at TIMESTAMPTZ), `chat_sessions` (id UUID DEFAULT uuid_generate_v4() PRIMARY KEY, user_id UUID REFERENCES profiles(id) ON DELETE CASCADE, title TEXT NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW()), and `chat_messages` (id UUID DEFAULT uuid_generate_v4() PRIMARY KEY, session_id UUID REFERENCES chat_sessions(id) ON DELETE CASCADE, role TEXT NOT NULL CHECK (role IN ('user', 'assistant', 'system')), message TEXT NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW()) with indexes on `user_id` and `session_id`. Enable RLS on `profiles`, `chat_sessions`, and `chat_messages` restricting `SELECT`, `INSERT`, `UPDATE`, and `DELETE` strictly to `auth.uid() = user_id` (via user_id ownership or session joins). Implement `POST /api/chat`, `POST /api/chat/session`, `GET /api/chat/history`, `GET /api/chat/session/:id`, `DELETE /api/chat/session/:id`, `POST /api/auth/sync`, and `DELETE /api/chat/history`. Server-side `@google/genai` instantiation using `process.env.GEMINI_API_KEY`; never expose keys to client; enforce JWT auth middleware and express rate limiting on all `/api/chat/*` routes. AI System Prompt: "You are an expert Resume Reviewer, Career Coach, ATS Specialist, Technical Interviewer, and Software Engineering Mentor. Responsibilities: Resume analysis, ATS optimization, career guidance, interview preparation, skill gap analysis, learning roadmap creation, project recommendations. When resume context is available, answer based on the user's actual resume. When job description context is available, tailor advice to that role. Never hallucinate resume information. If information is missing, state it clearly. Provide concise, actionable, professional responses strictly matching requested structured output formats when requested." Define shared Zod schema validating `message` (1-4000 chars), `session_id` (UUID string), optional `resume_text`, optional `job_description`, and `history` array. Build `ChatWindow`, `MessageBubble`, `ChatInput`, `SuggestedQuestions`, `TypingIndicator`, `MarkdownRenderer`, `CodeBlockRenderer`, `CopyButton`, `NewChatButton`, `ChatHistorySidebar`, `Navbar`, `Sidebar`, `StatsOverview`, and modular Tailwind UI primitives (`Button`, `Card`, `Input`, `Select`). Configure CORS for Replit, read sensitive keys via env vars, sanitize inputs, enforce maximum message lengths, rate limit chat endpoints, and protect client routes with `AuthGuard`. Expect `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `PORT`, `SUPABASE_SERVICE_ROLE_KEY`, and `GEMINI_API_KEY`. Organize into `client/src` (components, pages, lib), `server` (routes, middleware, lib), and `shared/schemas.ts`. Implementation Order: Setup DB & Env -> Server & Auth -> Gemini API Chat Route -> React Auth & Routing -> Chat UI & History Sidebar -> E2E Verification. Ensure working auth, verified Zod validation, rate-limited server-side Gemini streaming generation, persisted PostgreSQL chat records, and dynamic ChatGPT-style UI rendering. Build every file entirely now following this spec with full functional code and zero stubs or placeholders.